# Copy to notify.env (not tracked by git) before running install.sh.
# Separate multiple addresses with spaces or commas.
NOTIFY_EMAIL="you@oregonstate.edu"
# Sender address (default gliderhelper@<fully-qualified hostname>).
NOTIFY_FROM="gliderhelper@gliderfs3.ceoas.oregonstate.edu"
